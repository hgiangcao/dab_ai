Task: Refactor bots/alpha_beta.py for Iterative Deepening & Endgame SolvingRewrite the AlphaBetaPlayer in bots/alpha_beta.py to implement Iterative Deepening with a strict time limit and optimized move ordering. Apply the following technical constraints:  1. Class Initialization & Strategy GateUpdate __init__ to accept time_limit: float = 2.0 and endgame_threshold: int = 12. Remove the static depth parameter.  In get_move(s), check the number of empty lines. If > endgame_threshold, return an immediate 3-line box capture or a random safe move.  If <= endgame_threshold, execute Iterative Deepening.2. Iterative Deepening WrapperCreate a new method _iterative_deepening(self, s_node).Calculate end_time = time.time() + self.time_limit.Loop current_depth from 1 up to the number of valid moves.In a try/except TimeoutError block, call alpha_beta_search.Maintain a best_move_overall variable. Update it only when a depth iteration fully completes. Return this move upon timeout or reaching maximum depth.3. Timeout InjectionPass end_time into alpha_beta_search.  At the start of alpha_beta_search, check if time.time() > end_time: raise TimeoutError().4. Optimized Move OrderingInside alpha_beta_search, before the search loop, sort valid_moves strictly in this order:  TT Hit: tt_best_move (if available).  Greedy Captures: Moves that complete a 3-line box (calculate this dynamically).Remaining: All other valid moves (shuffled).Remove the current random shuffle implementation that only prioritizes the TT move.  5. Zobrist State PersistenceEnsure the ZobristHash instance is created inside get_move and passed down through the iterative deepening loop so the TT bounds carry over between depths.  